import prisma from './../prisma';
import { Request, Response } from 'express';

class CUser {

  public getAuthenticatedUser = (req: Request, res: Response, next: Function) => {

  }

}

export default new CUser();